# SATARK Frontend

React 19 + Vite 8 frontend for the SATARK natural-disaster risk intelligence hackathon project.

## Run

```bash
npm install
npm run dev
```

Build:

```bash
npm run build
```

The app uses simulated demo data. Replace `src/data/demoData.js` and later `src/services/` with backend API data.
